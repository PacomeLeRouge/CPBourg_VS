namespace CPBourg.NextGenGui.Models
{
    /// <summary>Operator-facing measurement system. Machine data remains metric internally.</summary>
    public enum MeasurementUnit
    {
        Millimeters,
        Inches,
    }
}
