<UserControl x:Class="CPBourg.NextGenGui.Views.GlobalMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<UserControl.Resources>
		<!-- Balanced menu typography: still larger than the original pass,
             but compact enough to fit in shorter/windowed views. -->
		<Style x:Key="GlobalMenuSectionHeaderTextStyle" TargetType="TextBlock">
			<Setter Property="FontSize" Value="14" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Foreground" Value="{StaticResource JobsAccentBrush}" />
		</Style>

		<Style x:Key="GlobalMenuIconTextStyle" TargetType="TextBlock">
			<Setter Property="FontFamily" Value="{StaticResource IconFontFamily}" />
			<Setter Property="FontSize" Value="20" />
			<Setter Property="Width" Value="32" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>

		<Style x:Key="GlobalMenuItemTextStyle" TargetType="TextBlock">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontWeight" Value="SemiBold" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>
	</UserControl.Resources>

	<!--
        Slide-out global navigation, opened from the hamburger button in the
        app shell header (see MainWindow). Grouped sections match the
        approved reference: General / Configuration / Advanced / Help.

        Section headers use the blue Jobs accent with a rule line underneath
        for a distinct separation, and nav item text is bold/darker for more
        pronounced legibility, per feedback.

        Items are static placeholders for the prototype - wire Click to real
        navigation once the corresponding screens exist (only Home and this
        dashboard exist today).
    -->

	<Grid>
		<!-- Scrim: click outside the panel to close -->
		<Border Background="{StaticResource OverlayScrimBrush}" MouseDown="OnScrimMouseDown" />

		<!-- Panel -->
		<Border Width="370" HorizontalAlignment="Left"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,1,0">
			<DockPanel Margin="24,22,24,22">
				<Grid DockPanel.Dock="Top" Margin="0,0,0,18">
					<TextBlock x:Name="MenuTitleText" Text="Global Menu" FontSize="24" FontWeight="Medium"
                               Foreground="{StaticResource TextPrimaryBrush}" />
					<Button HorizontalAlignment="Right" Width="48" Height="48" Click="OnCloseClick">
						<TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFontFamily}" FontSize="20" />
					</Button>
				</Grid>

				<StackPanel>
					<!-- GENERAL -->
					<StackPanel Margin="0,0,0,7">
						<TextBlock x:Name="GeneralHeaderText" Text="GENERAL" Style="{StaticResource GlobalMenuSectionHeaderTextStyle}" />
						<Border Height="2" Background="{StaticResource JobsAccentBrush}" Margin="0,4,0,0" />
					</StackPanel>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Home">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE80F;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="HomeNavText" Text="Home" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Job / File Menu">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE8A5;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="JobsNavText" Text="Job / File menu" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Error &amp; Information">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE7BA;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="ErrorsNavText" Text="Error &amp; Information" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Settings / Preferences">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE713;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="SettingsNavText" Text="Settings / Preferences" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>

					<!-- CONFIGURATION -->
					<StackPanel Margin="0,14,0,7">
						<TextBlock x:Name="ConfigurationHeaderText" Text="CONFIGURATION" Style="{StaticResource GlobalMenuSectionHeaderTextStyle}" />
						<Border Height="2" Background="{StaticResource JobsAccentBrush}" Margin="0,4,0,0" />
					</StackPanel>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Machine Line Configuration">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE9A9;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="MachineLineNavText" Text="Machine Line Configuration" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>

					<!-- ADVANCED -->
					<StackPanel Margin="0,14,0,7">
						<TextBlock x:Name="AdvancedHeaderText" Text="ADVANCED" Style="{StaticResource GlobalMenuSectionHeaderTextStyle}" />
						<Border Height="2" Background="{StaticResource JobsAccentBrush}" Margin="0,4,0,0" />
					</StackPanel>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Technician Interface">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE72E;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="TechnicianNavText" Text="Technician Interface" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>

					<!-- HELP -->
					<StackPanel Margin="0,14,0,7">
						<TextBlock x:Name="HelpHeaderText" Text="HELP" Style="{StaticResource GlobalMenuSectionHeaderTextStyle}" />
						<Border Height="2" Background="{StaticResource JobsAccentBrush}" Margin="0,4,0,0" />
					</StackPanel>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="Help / Manual">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE897;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="HelpNavText" Text="Help / Manual" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>
					<Button Style="{StaticResource NavItemButtonStyle}" MinHeight="50" Click="OnNavItemClick" Tag="About / Version">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="&#xE946;" Style="{StaticResource GlobalMenuIconTextStyle}" />
							<TextBlock x:Name="AboutNavText" Text="About / Version" Style="{StaticResource GlobalMenuItemTextStyle}" />
						</StackPanel>
					</Button>
				</StackPanel>
			</DockPanel>
		</Border>
	</Grid>
</UserControl>
